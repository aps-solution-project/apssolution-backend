spring:
  application:
    name: apssolution-backend

  datasource:
    url: jdbc:mysql://database-2.clwoya2662ja.ap-northeast-2.rds.amazonaws.com:3306/apssolution
    username: admin
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 300000       # 5분 이상 유휴 커넥션 제거
      max-lifetime: 1800000      # 30분, 서버 wait_timeout보다 짧게
      connection-timeout: 30000
      validation-timeout: 3000
      leak-detection-threshold: 60000

  jpa:
    show-sql: false

  jackson:
    default-property-inclusion: NON_NULL

  mail:
    host: smtp.gmail.com
    port: 587
    username: JHwebDevelopmentCom@gmail.com
    password: ${emailPw}
    properties:
      mail:
        smtp:
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 500MB


server:
  tomcat:
    max-part-count: -1